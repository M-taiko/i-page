Never implement a feature that is specific to one industry.

Always prefer generic naming.

Always think Multi Tenant.

Every page must work on Mobile before Desktop.

No page may require horizontal scrolling.

Every module must be independently removable.

Every component must be reusable.

Every feature must expose Analytics.

Every QR must be trackable.

Every Organization can enable or disable Modules.

No Business Logic inside Blade.

No SQL inside Controllers.

No duplicated UI Components.

Every Feature must have permissions.

Every Feature must support RTL.

Every Feature must support Dark Mode.

Every Feature must support Accessibility.

Every database table must support auditing.

Every public feature must consider Guest users.

Every API must be versioned.

Every implementation should be AI-ready.

Always think SaaS.

Always think Enterprise.

Always think Global.